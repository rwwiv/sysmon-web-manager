export type Agent = {
    hostName: string,
    macAddress: string,
    ipAddress: string,
    uuid: string,
    sysmonVersion: string,
    configName: string,
    lastOnline: Date,
    sysmonRunning: boolean
}
